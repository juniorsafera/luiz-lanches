import 'package:flutter/material.dart';

class TelaCarrinho extends StatelessWidget {
   
  const TelaCarrinho({ Key? key,
  
   }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(children:const [
      Text('Carrinho')
    ],);
  }
}